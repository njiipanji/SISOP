PROXY
> atur proxy:  export https_proxy="http://username:password@proxy.its.ac.id:8080/"

GIT 15
masuk ke github
> register di github.com
> membuat repositories baru
> copy link
> add collaborator

perintah2 di terminal
> git clone alamatfile -> membuat clone dari repositories ke PC
> git add namafile -> menambahkan file untuk ditrack
> git commit -a -> menyetujui perubahan
> git push -> upload ke repositories
> git pull -> download yang terbaru dari repositories...

cek versi git
git --version

install git
sudo -E apt-get install git

ATURAN
1. git pull
2. tiap orang jangan edit di file yang sama, jadi harus file beda yang diedit di waktu yang bersama
3. edit file / add file ke dalam folder git
4. git add "namafile"
5. user harus memasukkan username.email & username.git sebelum ngepush ke git
	git config --global user.email "emailgit" --> 
	git config --global user.name "usernamegit" -->
6. git commit -a --> untuk membuat comment tentang perubahan/update dari file yang akan di push.
7. git push --> untuk mengupload/update file yang sudah ditambahkan di folder. 

STRING
> strstr -> mencari kemunculan dari karakter pertama
	ex: hello world -> cari "o"	-> output: o world
> strtok -> memparsing sebuah string dengan sebuah karakter (dipecah2)
	ex: hello world -> parsing "o"	-> 'hell' ' w' 'rld'
// codingan ada di folder

THREAD
> POSIX Thread	-> harus return value (void*), pointer cuma 1 (*void)
> pthread_create(variabel pthread, NULL,nama fungsi yang ingin dijalankan, NULL);
> compile : gcc -o fileexecutable filesourcecode -lpthread
> scheduling thread : penjadwalan kapan thread dimulai terlebih dahulu itu tergantung Sistem Operasi masing2, namun apabila thread di main sudah berhenti, maka yang lain juga berhento
> pthread_join(variable pthread, NULL) : perintah untuk menunggu thread yang lain selesai terlebih dahulu sebelum program mati
